# Copie para .env.local e ajuste se necessário.
# Somente variáveis com prefixo VITE_ chegam ao navegador.
# Nunca coloque credenciais aqui: elas ficam apenas no backend.

VITE_API_URL=http://localhost:8000
